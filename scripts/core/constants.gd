class_name CoreConstants

enum BloomColor {WHITE, RED, PINK, PURPLE, YELLOW}

const BaseColors = [Color.WHITE, Color.RED, Color.HOT_PINK, Color.REBECCA_PURPLE, Color.YELLOW]

const FontColors = [Color.BLACK, Color.WHITE, Color.BLACK, Color.WHITE, Color.BLACK]

const TIER_PLANTS = [40, 30, 20]

const TIER_COLORS = [Color.GREEN, Color.GOLD, Color.BLUE, Color.CHARTREUSE]

enum Action {SHOP, FORAGE, WILD, GROW}

enum GameState {LOBBY, LIVE, FINAL, END}
